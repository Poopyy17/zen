/**
 * Owns all form functionality (React Hook Form + Zod), per tenant.
 * NOT YET IMPLEMENTED — the RHF-bound Form/FormField/FormControl/FormMessage
 * wrappers and per-tenant form definitions are real design/UI work beyond
 * initial package scaffolding. See PROJECT_OUTLINE.md — `form-library`.
 */
export {};
