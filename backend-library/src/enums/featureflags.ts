/**
 * Canonical catalog of feature flag keys, shared by every service.
 * Per-tenant values live in the `platform` database (see PROJECT_OUTLINE.md
 * — Handling Per-Tenant Hotfixes on a Shared Deployment).
 */
export enum FeatureFlag {
  // Example placeholder — replace with real flags as they're introduced.
  HotfixX = "hotfixX",
}
