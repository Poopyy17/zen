PORT=3001
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-url>/
PLATFORM_DB_NAME=test-tenant1
PLATFORM_JWT_SECRET=<random-secret>
# Optional — only read by scripts/seed-super-admin.mjs. Omit to get a random
# generated password (printed once) instead of a fixed one.
SUPER_ADMIN_PASSWORD=<dev-only-password>
